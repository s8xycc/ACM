#include <stdio.h>
int main()
{
    long long n,m;
    double sum=0;
    while(scanf("%lld%lld",&n,&m)==2&&n!=m&&n!=0&&m!=0)
    {
        while(n<=m)
        {
            sum+=1/(double)(n*n);
            n++;
        }
        printf("%.5lf\n",sum);
    }
}
