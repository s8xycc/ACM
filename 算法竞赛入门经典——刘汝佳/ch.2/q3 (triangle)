#include <stdio.h>
int main()
{
    int n;
    scanf("%d",&n);

    for(int i=0;i<n;i++)
    {
        int t=i;
        while(t--) printf(" ");
        int j=2*(n-i)-1;
        while(j--) printf("#");
        printf("\n");
    }
}
